version: '3.8' # 이 줄은 선택적으로 제거 가능 (경고 피하려면)

services:
  flask-web:
    build: .
    container_name: naver-map-flask
    ports:
      - "8080:5050"
    restart: always
    # 🚨 수정: env_file로 .env 파일 전체를 주입
    env_file:
      - .env 